import {memo} from 'react'

import {Divider} from '$commons/components/Divider'

export const WebViewHeaderShadow = memo(function WebViewHeaderShadow() {
    return <Divider css={{height: '1px', backgroundColor: 'button'}} />
})
